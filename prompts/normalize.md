Given these thematic labels assigned to observations about a person's thinking patterns, identify synonymous or overlapping labels and produce a canonical mapping.

Rules:
- Keep the most descriptive label as the canonical form
- Only merge labels that genuinely describe the same pattern — near-misses are not matches
- Labels that are distinct should remain separate even if thematically related
- Output format: one line per merge, "original label → canonical label"
- Labels with no synonyms: omit them (they map to themselves)
- No commentary, just the mapping lines
